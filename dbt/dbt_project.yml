name: 'market_intelligence'
version: '1.0.0'
config-version: 2

profile: 'market_intelligence'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"